# Titan POS — no custom shrinking rules yet. Release build keeps minify off
# (see build.gradle.kts); this file exists so the release proguardFiles entry
# resolves. Add keep rules here when release minification is enabled.
